"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#b7daf2",
      main: "#5dade2",
      dark: "#007fce",
      contrastText: "#fff",
    },
  },
});

export default theme;
